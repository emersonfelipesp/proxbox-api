NEXT_PUBLIC_PROXBOX_API_URL=http://127.0.0.1:8000
NEXT_ALLOWED_DEV_ORIGINS=10.0.30.207

# The FastAPI app must allow your browser origin (CORS). Defaults include http://localhost:3000.
# For other UI hosts, set on the API: PROXBOX_CORS_EXTRA_ORIGINS=https://my-ui.example,http://10.0.0.5:3000
