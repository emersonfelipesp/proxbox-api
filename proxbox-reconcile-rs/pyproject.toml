[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "proxbox-reconcile-rs"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
python-source = "python"
module-name = "proxbox_reconcile_rs._native"
features = ["pyo3/extension-module"]
